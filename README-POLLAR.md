# Pollar — 1 USDC de activación (Stellar)

Pollar **no** alimenta el pozo de Avalanche. Es la rampa: 1 USDC mainnet activa el gremio; después Unlock abre `/portal` y el riel EVM.

Idea alineada al bounty: pasanaku cotidiano / gremio.

## Flujo (dentro de `/join`, no un link suelto)

1. Login Pollar (Google).
2. **Activar gremio — 1 USDC**.
3. Hash grande en pantalla + este README.
4. Grant Key Unlock a mano si hace falta.

Red sandbox: `NEXT_PUBLIC_POLLAR_NETWORK=testnet`  
Mainnet (una sola vez): `NEXT_PUBLIC_POLLAR_NETWORK=mainnet` y `NEXT_PUBLIC_POLLAR_MAINNET_ARMED=true`

## Hash mainnet (GATE)

```
_
```

Stellar Expert: `https://stellar.expert/explorer/public/tx/<HASH>`

**No repetir la tx.** Presupuesto $5.

## Destino

`NEXT_PUBLIC_POLLAR_DESTINO` = address Stellar `G…` (la tuya o la del bounty).
